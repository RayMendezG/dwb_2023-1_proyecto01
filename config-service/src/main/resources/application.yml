server:
  port: 8888
  
spring:
  cloud:
    config:
      server: 
        git:
          uri: https://github.com/RayMendezG/config-data
          search-paths: api
          
          default-label: "main"
          
  security:
    user:
      name: root
      password: adminadmin